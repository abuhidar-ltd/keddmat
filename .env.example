# Used for "share store" full URLs in the merchant dashboard. Omit trailing slash.
# Leave unset in local dev to use http://localhost:PORT automatically.
# VITE_PUBLIC_SITE_URL=https://your-production-domain.com
